# 2026-03-21 工作日志

## 热点追踪系统 V3 优化

### 改动概要
对 `/Users/yulu/CodeBuddy/20260319112043/.codebuddy/skills/daily-hotspot/` 下的核心文件进行了三大优化：

### 1. 内容摘要优化 (`content_analyzer.py` → `build_detail_brief`)
- **问题**：旧版只复述标题，读完还是不知道热点说了什么
- **改法**：新逻辑会收集 summary/content 中的真实描述，拼成"标题核心 + 关键描述句（前2~3句）"，确保摘要≠标题
- **效果**：如 `langchain-ai/open-swe` 的摘要从"AI 编程/代码辅助工具"变成了"An Open-Source Asynchronous Coding Agent"

### 2. 精选话题结论重写 (`content_analyzer.py` → `build_content_strategy` 及新增函数)
- **问题**：旧版结论只是泛标签拼接，无法直接执行
- **改法**：新增 `_derive_priority_reason` / `_derive_platform_reason` / `_build_angle_detail` 三个函数
  - 话题类型：保持不变
  - 可用角度：带具体热点名 + 技术方向 + 产品关联方式 + 竞品对比说明
  - 优先级：附带时效解释（如"竞品动态时效性强"）
  - 适合平台：附带具体动作（如"直接参与讨论"、"发教程文章"）
- **效果**：可用角度从 `技术科普 + 教程拆解` 变成了 `「OpenCode」涉及 Agent 技术方向 + 产品关联（CodeBuddy） + 教程拆解（结合 CodeBuddy 做实操内容）`

### 3. 文案只输出英文 (`daily_hotspot_agent.py` → `build_writing_pack_markdown`)
- **问题**：中英文都输出太冗余，且中文文案不好用
- **改法**：写作包 Markdown 中只保留英文文案（X / Discord / LinkedIn / Reddit 四个平台）
- CSV 仍保留中英文双语字段不变（方便表格查看）

### 输出位置
- V3 输出：`/Users/yulu/CodeBuddy/20260319112043/.codebuddy/skills/daily-hotspot/scripts/demo-output-v3/`

---

## 热点追踪系统 V6 优化（第二轮）

### 改动概要
继续对 `content_analyzer.py` 和 `daily_hotspot_agent.py` 进行深度优化：

### 1. 深度内容抓取（`daily_hotspot_agent.py` → 新增 `enrich_items_with_deep_content`）
- **问题**：内容摘要只能从已有的 summary/title 字段拼凑，信息量不够
- **改法**：在分析之前新增深度内容抓取步骤
  - GitHub：通过 `raw.githubusercontent.com` 抓取 README.md 全文
  - V2EX / HN / 36kr / ProductHunt / AI newsletters：抓取原文页面内容
  - 并发抓取（max_workers=8），60秒超时
- **效果**：41/50 条热点成功获取了深度内容，V2EX 帖子可以看到完整帖子（如 AI 中转避雷帖的核心痛点和避雷建议）

### 2. 网页噪音清洗（`content_analyzer.py` → 新增 `_strip_web_noise`）
- **问题**：36kr 页面抓取到大量导航栏/页脚文本
- **改法**：新增 `_strip_web_noise` 函数，过滤 36kr 导航栏、微信分享提示、时间戳、"下一篇"之后内容等

### 3. 删除无用字段
- **删除「是否渲染焦虑」**：从 CSV headers 和行数据中移除
- **删除「内容写作包摘要」**：从 CSV headers 和行数据中移除
- **删除所有平台文案**：从 CSV 和写作包 Markdown 中移除所有 X/Discord/LinkedIn/Reddit 中英文文案列

### 4. 摘要增大
- 摘要截断限制从 320 字符增大到 500 字符
- 句子抽取从 3 句增加到 4 句，总长度上限从 280 增大到 400

### 输出位置
- V6 输出：`/Users/yulu/CodeBuddy/20260319112043/.codebuddy/skills/daily-hotspot/scripts/scripts/demo-output-v6/`

---

## 热点追踪系统 V9 优化（第三轮：消除产品关联幻觉）

### 改动概要
针对用户反馈"产品关联写得很有幻觉、竞品乱标"的问题，对 `content_analyzer.py` 进行全面重建：

### 1. 中文简介改为"具体内容摘要的中文翻译+精简版"
- **问题**：旧版中文简介基于规则模板独立生成（如"AI 编程/代码辅助工具"），和具体内容摘要无关
- **改法**：`_generate_cn_summary` 现在接收 `detail_brief` 参数，直接基于具体内容摘要翻译+截断
  - 中文为主的内容直接截断到 150 字
  - 英文为主的尝试关键词翻译，翻译效果不好则保留原文精简版
- **效果**：中文简介和具体内容摘要现在是同一内容的不同长度版本，所有来源都会生成

### 2. 产品关联彻底重建（基于用户提供的真实产品列表）
- **问题**：`TCLOUD_PRODUCT_RULES` 中关键词太宽泛（如 "agent"、"deploy"、"docker"、"bot" 都能命中 Lighthouse），导致几乎所有 AI 热点都被强行关联到 Lighthouse
- **改法**：
  - **重建 `TCLOUD_PRODUCT_RULES`**：基于用户提供的腾讯云真实产品列表（EdgeOne / Lighthouse / COS / TDSQL-C / ES / IM / TRTC / Hunyuan3D / CodeBuddy / 云直播 / CDN / SMS / SES / CVM / CBS），每个产品只匹配明确提到该产品的关键词
  - **移除通用词**：从 Lighthouse 中删除 "agent"/"bot"/"docker"/"deploy" 等通用词，从 EdgeOne 中删除 "cdn"/"edge"/"waf" 等通用词
  - **只有明确提到产品名才能命中**
- **效果**：Mamba-3 / OpenCode / langchain-ai/open-swe 全部正确显示「待人工判断」，不再强贴产品

### 3. 竞品标签收紧
- **问题**：`COMPETITOR_TAGS` 太宽泛，如 "workers" 命中 Cloudflare、"r2" 命中 Cloudflare R2、"meta/facebook/llama" 命中 Meta（Ubuntu sudo 文章因出现 cursor 被标为 Cursor 竞品）
- **改法**：
  - `PRODUCT_TAGS`：Cursor 改为 "cursor ai/ide/editor"（不再匹配普通英文词 cursor）；Copilot 改为 "github copilot"
  - `COMPETITOR_TAGS`：移除 Google/Meta（太宽泛），移除 "workers"/"r2"/"lambda"/"ec2"/"s3"/"eks" 等通用词
  - `COMPETITOR_PRODUCT_TAGS`：同步收紧
- **效果**：Ubuntu sudo 文章竞品标签正确为空

### 4. 36kr 噪音清洗增强
- **问题**：36kr 的 summary 字段也包含导航噪音（之前只清洗了 content）
- **改法**：`build_detail_brief` 中对 summary 也走 `_strip_web_noise` 清洗

### 输出位置
- V9 输出：`/Users/yulu/CodeBuddy/20260319112043/.codebuddy/skills/daily-hotspot/scripts/scripts/demo-output-v9/`

### 用户提供的腾讯云产品列表（写入 MEMORY.md）
EdgeOne / Lighthouse / COS / TDSQL-C / ES / IM / TRTC / Hunyuan3D / CodeBuddy / 云直播 / CDN / SMS / SES / CVM / CBS

---

## 热点追踪系统 V10 优化（第四轮：应用场景关联 + 中文翻译 + 云厂商竞品）

### 用户反馈
1. 中文简介是具体内容摘要的**翻译**（不是截断）
2. 产品关联是基于**应用场景**的（不是只匹配产品名）
3. 竞品只指海内外**云厂商**（不是 OpenAI/Anthropic）

### 改动
1. `_generate_cn_summary`：对整段 detail_brief 做关键词翻译
2. `_POLISH_MAP`：从 ~50 词增加到 ~120 词，支持长短语优先匹配
3. `TCLOUD_PRODUCT_RULES`：改为场景驱动（如 agent → Lighthouse, AI 编码 → CodeBuddy）
4. `COMPETITOR_TAGS`：只保留云厂商，移除 OpenAI/Anthropic
5. `COMPETITOR_PRODUCT_TAGS`：只保留云厂商具体产品

### 输出位置
- V10 输出：`/Users/yulu/CodeBuddy/20260319112043/.codebuddy/skills/daily-hotspot/scripts/scripts/demo-output-v10b/`

---

## 热点追踪系统 V12 优化（第五轮：翻译质量 + 泛化消除 + newsletter 处理）

### 用户反馈
1. 中文翻译还有大量半中半英，如 `Agent 技能 框架 & software 开发方法论 好用的`
2. V2EX/newsletter 的腾讯云结合点/写作角度/配图建议还在用通用模板，如 `**该话题** 支持自部署`
3. Newsletter 中文简介经常被截断（如 `What makes a good A`）

### 改动（`content_analyzer.py`）

#### 1. `_POLISH_MAP` 大幅扩展（~120 → ~270+ 词条）
- 修复 `"that works"` → 空字符串（无意义短语）
- 添加完整短语级翻译：`"what makes a good"→"怎样写好"`, `"the fastest way to ship"→"最快发布"`, `"practice tough phone calls with AI before you make them"→"用 AI 练习棘手电话"` 等
- 补充 V12b 遗漏词：`shows→展示`, `simulation→仿真`, `apps→应用`, `way→方式`, `power→能力`, `any→任何`, `via→通过`, `launches→启动`, `makes→使得`, `loves→看好` 等
- 补充 newsletter 常见词：`open models→开源模型`, `government control→政府管控`, `building the→打造` 等
- 移除重复键 `"manage"` 和重复条目 `"real-time"`

#### 2. `_DESC_PHRASE_MAP` 修复（V12a 已完成）
- 所有模式添加 `^` 锚点防止句中误匹配（如 "AI fast" 不再错误匹配 "高性能的"）

#### 3. `_translate_en_to_cn` 完全重写（V12a 已完成）
- 新增 `&` → `和` 预处理
- 分句翻译：逐句翻译后用 `。` 拼接
- 两级虚词清理：cn_ratio ≥ 15% 轻度（冠词/介词），≥ 30% 激进（所有虚词）
- 后处理：清理 `和 和` 重复、双标点、连续句号

#### 4. Newsletter 中文简介处理重建
- **问题**：旧逻辑走 `_translate_en_to_cn`，它有 cn_ratio ≥ 0.3 门槛，短标题（< 60 字符）翻译不达标会被截断
- **新逻辑**：短标题（< 60 字符）直接走 `_polish_english_fragment` 关键词翻译 + 虚词清理，绕开 cn_ratio 门槛
  - 虚词清理列表：with/for/the/a/an/and/or/of/to/in/on/is/are/i'd/i/like/try/this/that/from/by/it/its
- **效果**：`What makes a good A` → `怎样写好 Agent.md?`; `GPT 5.4 is a big st` → `GPT 5.4 的一大进步： Codex`; `Nvidia loves OpenCl` → `Nvidia 看好 OpenClaw`

#### 5. ProductHunt 中文简介处理新增
- **问题**：ProductHunt tagline 走 `_translate_en_to_cn` 效果差（cn_ratio 不达标）
- **新逻辑**：直接对 tagline 做 `_polish_english_fragment` + 虚词清理
  - 去掉 `"ProductName:"` 前缀后翻译
  - 翻译后清理英文虚词
- **效果**：`最快的 way 发布 出色的 ChatGPT apps` → `最快发布 出色的 ChatGPT 应用`

#### 6. emoji 清理改用通用 Unicode 范围
- 旧：硬编码 emoji 列表（容易遗漏）
- 新：`[\U00010000-\U0010ffff]` + `[\u2600-\u27bf\u2300-\u23ff]` 覆盖所有 emoji

#### 7. 泛化问题消除（V12a 已完成）
- 新增 `_extract_topic_phrase()` 提取话题关键词替代 `"该话题"`
- 新增 `is_discussion` 标志，V2EX/newsletter/36kr 使用讨论类模板
- 腾讯云结合点：`「AI 中转帖子占领了」话题涉及自部署需求，可以切入 Lighthouse 的一键部署能力做产品种草`
- 写作角度：`以「AI 中转帖子占领了」话题为切入点，写一篇『开发者为什么需要自己的轻量服务器』的观点文章`
- 配图建议：`主视觉用 Lighthouse 控制台截图 + 开发者使用场景插画`

### 输出位置
- V12d 最终输出：`/Users/yulu/CodeBuddy/热点追踪/demo-output-v12d/`
- 中间版本：`demo-output-v12/`, `demo-output-v12b/`, `demo-output-v12c/`

### 已知限制（规则引擎固有）
- 部分长英文句子无法完美翻译（如 "Dean Ball open 模型 government control"）
- Memia 标题本身是碎片化关键词列表，翻译效果有限
- `"Everest AI：Building 世界's 最快的 IPMI 单板计算机"` 中 "Building" 未翻译（需要上下文感知）

---

## 热点追踪系统 V13 优化（第六轮：Google Translate 替代规则引擎 + 社媒结论差异化）

### 用户反馈
1. 社媒结论太同质化 —— 几乎每条都是 `"围绕 XX 做内容，切入方式：…"` 同一个模板
2. 中文翻译方法根本不对 —— 不应该用「关键词提取+拼装」，而应该直接整句翻译英文

### 改动（`content_analyzer.py`）

#### 1. 引入 Google Translate API 替代规则引擎翻译
- **安装**：`pip3 install googletrans==4.0.0-rc1`
- **新增函数**：`_get_translator()` / `_google_translate(text)` —— 懒加载 Google Translate，失败时静默回退
- **重写 `_generate_cn_summary`**：
  - 新流程：提取英文摘要 → Google Translate 整句翻译 → 失败时回退到规则引擎
  - 新增 `_extract_en_summary()` 函数：从 detail_brief 中提取干净的 1-2 句英文摘要
    - newsletter：优先用标题（去掉期号前缀和 emoji）
    - producthunt：去掉 `"ProductName:"` 前缀取 tagline
    - 通用：按句拆分，取前 2 句有意义描述，过滤网站噪音（Download/Sign/curl/npm 等）
  - 翻译结果自动加项目名前缀（如果翻译中没有的话）
- **效果大幅提升**：
  - `TradingAgents`：`大语言模型开发框架/平台`（错误）→ `多代理LLM金融交易框架` ✅
  - `claude-hud`：`Claude Code 插件 展示 运行状态` → `一个 Claude Code 插件，显示正在发生的事情` ✅
  - `murmur`：完全未翻译 → `在拨打电话之前先用人工智能练习一下艰难的电话` ✅
  - `ChinAI #351`：`CAICT 启动 2026 AI 安全评估` → `中国信通院启动2026年人工智能安全评估` ✅
  - `Everest AI`：`Building 世界's 最快的 IPMI 单板计算机` → `打造世界上最快的 IPMI 单板计算机` ✅

#### 2. 社媒结论完全重写（新增 `_build_social_recommendation` 函数）
- **旧逻辑**：固定模板 `"围绕 XX 做内容，切入方式：{angle_detail}。优先级 XX，推荐平台：XX。"`
- **新逻辑**：根据 产品类型 × 内容特性 × 来源类型 动态组合差异化结论
  - **无产品关联**：根据话题类型（竞品动态/社区热议/其他）给不同建议
  - **CodeBuddy**：区分竞品对比 / 讨论话题 / 通用
  - **Lighthouse**：区分自部署 / 竞品 / 开源克隆 / GPU 计算 / 金融交易 / AI 编程 / 文档处理 / 插件扩展 / 讨论话题 / 通用
  - **EdgeOne**：区分安全 / 讨论 / 通用
  - **其他产品**：按 Hunyuan3D/COS/TDSQL-C/ES/CVM/IM/TRTC 分别对应不同场景描述
- **效果**：
  - `TradingAgents`：`围绕 Lighthouse 做内容…` → `是金融/交易类 AI 项目，可写『用 Lighthouse 低成本跑量化 Agent』的实战分享`
  - `opendataloader-pdf`：`围绕 Lighthouse 做内容…` → `涉及文档处理，可写『Lighthouse 部署 opendataloader-pdf 搭建私有文档解析服务』的教程`
  - `TaxHacker`：`围绕 Lighthouse 做内容…` → `支持自部署，可写『Lighthouse 一键部署 TaxHacker』实操教程`

### 输出位置
- V13b 最终输出：`/Users/yulu/CodeBuddy/热点追踪/demo-output-v13b/`

### 依赖变更
- 新增 `googletrans==4.0.0-rc1`（通过 pip3 安装，非项目 requirements.txt）

---

## 热点追踪系统 V14 修复（第七轮：36kr 页脚噪音导致竞品误标）

### 用户反馈
36kr 抓取的新闻（脑机接口、稀土矿、车企销量、财政政策等）被错误标记为「竞品动态」，与 AI / 云计算毫无关系。

### 根因
36kr 页面页脚包含 **"本站由 阿里云 提供计算与安全服务"** 版权文本，混入了 content 字段。导致：
1. **所有 36kr 文章都命中「阿里云」竞品标签** → 被标为竞品动态
2. 导航栏的「智能涌现」等栏目名命中了 AI 关键词 → is_ai=True
3. 这些文章通过了 `filter_non_ai_non_cloud_items` 过滤（is_ai=True 或 is_cloud=True）

### 改动（`content_analyzer.py`）

#### 1. `_strip_web_noise` 增加 36kr 页脚清洗
- 清除 `"本站由 阿里云 提供计算与安全服务"` 及后续所有版权文本
- 清除 `"热门产品 文章标签 快讯标签 合作伙伴 36氪APP下载"` 块
- 清除 `"36氪Auto 数字时氪 未来消费 智能涌现..."` 完整导航头
- 清除 `©` 版权声明及 `鲸准/氪空间` 广告块

#### 2. `_text_for_analysis` 加入噪音清洗
- 对 summary 和 content 调用 `_strip_web_noise()` 后再拼接，避免页脚品牌名污染关键词匹配

### 效果
- 过滤前：removed=9, kept=41（脑机接口等全部保留 ❌）
- 过滤后：removed=17, kept=33（脑机接口/稀土矿/财政政策/车企/阿维塔全部过滤 ✅）

### 输出位置
- 修复后输出：`/Users/yulu/CodeBuddy/热点追踪/output/`
